all:
  hosts:
    vm-mon1:
      ansible_host: 111.88.157.49
      ansible_user: nbuzik
      ansible_ssh_private_key_file: "{{ lookup('env', 'ANSIBLE_SSH_KEY') | default('/home/nbuzik/.ssh/ansible_key', true) }}"
